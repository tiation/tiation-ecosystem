import SwiftUI

struct ProfileDetailView: View {
    var body: some View {
        VStack {
            Text("Profile Detail")
                .font(.largeTitle)
                .foregroundColor(.cyan)
            Spacer()
        }
        .background(Color.black)
        .padding()
    }
}
